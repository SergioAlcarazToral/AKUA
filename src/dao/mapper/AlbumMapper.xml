<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
  'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace='dao.mapper.AlbumMapper'>
	<select id='getAlbum'>
		SELECT * FROM album where nombre like '%#{nombre}%'
	</select>
	
	<select id='getAlbumes'>
		SELECT * FROM album
	</select>

	<insert id='insertAlbum' parameterType='Album'
		useGeneratedKeys='true' keyProperty='id'>
		INSERT INTO album(nombre,anyo,idArtista,foto) values('#{nombre}','#{anyo}','#{artista}','#{foto}')
	</insert>

	<update id='updateAlbum' parameterType='Album'>
		UPDATE album
		SET
		nombre = #{nombre},
		anyo = #{anyo},
		idArtista = #{artista},
		foto = #{foto}
		WHERE id = #{id}
	</update>

	<delete id='deleteAlbum' parameterType='Album'>
		DELETE FROM album WHERE id = #{id}
	</delete>


</mapper>